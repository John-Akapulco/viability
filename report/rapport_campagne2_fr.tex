\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage{geometry}
\geometry{margin=2.5cm}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,fit}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{amsmath}
\usepackage{longtable}

\sloppy
\tolerance=9999
\emergencystretch=3em

\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue,citecolor=blue}

\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  backgroundcolor=\color{gray!8},
  columns=fullflexible,
}

\graphicspath{{figures_campaign2/}}
\setlength{\headheight}{14pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Campagne 2 --- Descripteurs ICOHP et stabilité thermodynamique}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\title{\bfseries Descripteurs ICOHP et stabilité thermodynamique \\
sur un jeu de 597 structures \\
\large Campagne 2 : construction du jeu de données, calcul VASP+LOBSTER,
et analyse statistique}
\author{Gilles Frapper \\ Professeur des Universités, Chimie Théorique \\ IC2MP UMR 7285 CNRS, Université de Poitiers \\ \href{mailto:gilles.frapper@univ-poitiers.fr}{gilles.frapper@univ-poitiers.fr}\\[4pt] \small Rapport généré dans le cadre du projet \texttt{viability}}
\date{16 août 2026}

\begin{document}
\maketitle

\begin{center}
\small\itshape La question centrale du projet est la discrimination de
viabilité fondée sur $\Delta$(ICOHP) (\S\ref{sec:reframing}), sur un jeu
de 597 structures.
\end{center}

\tableofcontents
\newpage

\section{Objectif et problématique}

\textbf{Question de recherche centrale (voir \S\ref{sec:reframing}) : le
$\Delta$(ICOHP) --- descripteur ICOHP de réaction pour la décomposition
d'un composé en éléments, et en particulier la classification
endobondic/exobondic fondée sur son signe --- distingue-t-il les
composés thermodynamiquement stables, métastables et instables ?}

\section{Méthodologie}

\subsection{Sélection du jeu de données}

Requête Materials Project : systèmes chimiques unaires et binaires, non
magnétiques ($|\text{aimantation totale}| \leq 0.01~\mu_B$/unité
formulaire --- le champ catégoriel \texttt{magnetic\_ordering} de MP est
\og{}Unknown\fg{} pour $\sim$98\% des entrées et aurait exclu la
quasi-totalité des composés réellement non magnétiques), sans éléments
f. Le jeu de données combine deux logiques de
sélection complémentaires : un premier ensemble de 186 composés
($\leq 20$ atomes/maille, 60 par famille de stabilité, plafond de 2
entrées par système chimique pour garantir une diversité élémentaire
plutôt qu'une accumulation de polymorphes d'un même système), et un
second ensemble de 163 composés/éléments ($\leq 40$ atomes/maille)
ciblant spécifiquement des
références élémentaires supplémentaires, des polymorphes hors
équilibre (haute pression, allotropes) et des binaires alcalins/
alcalino-terreux contre N/O/F/P/S/Cl, choisis pour construire de vrais
groupes de polymorphes de même composition et tester les descripteurs
loin de l'équilibre ; et un troisième ensemble de 45 composés
binaires, appariant un composé expérimental métastable à la distance
maximale au-dessus du hull convexe à 1--3 polymorphes théoriques
encore plus éloignés du hull pour la même formule, un test de
robustesse délibérément aux cas limites de stabilité.

\subsection{Classification du type de liaison}

Le type de liaison (ionique / covalent / métallique) est calculé par
défaut par la fonction \texttt{classify()} (composition élémentaire +
\texttt{is\_metal}). Un second classement, indépendant, est construit
directement à partir des données ICOHP/ICOBI calculées pour chaque
composé (\S\ref{sec:dataset}) : \texttt{is\_metal} pour le caractère
métallique, puis l'ICOBI moyen par liaison (ordre de liaison,
covalence) pour distinguer ionique de covalent parmi les composés non
métalliques. Les deux classements sont comparés au
\S\ref{sec:dataset}.

\subsection{Calcul VASP + LOBSTER}

Même gabarit pour l'ensemble des structures (calcul statique,
\texttt{ISYM=$-1$}, \texttt{LASPH=.TRUE.}, potentiels PAW-PBE recommandés
par \texttt{pymatgen.io.vasp.sets.MPRelaxSet}) : \texttt{NBANDS} est
dérivé par composé du nombre d'orbitales locales de la base LOBSTER
(\texttt{Lobsterin.write\_INCAR}), et la détermination des fonctions de
base passe par \texttt{Lobsterin.get\_basis(potcar\_symbols=\ldots)}
plutôt que par le parsing direct du fichier POTCAR (dont la validation
interne de \texttt{pymatgen} échoue sur plusieurs POTCAR non reconnus par
empreinte de hachage dans le miroir local).

\subsection{Grandeurs ICOHP/ICOBI : intégration, coupures, unités}
\label{sec:icohp-methodo}

\textbf{Génération LOBSTER (\texttt{lobsterin}, identique sur tout le
jeu de données)} : base \texttt{pbeVaspFit2015} ; fenêtre d'intégration
COHP de $-35$ à $+5$~eV autour du niveau de Fermi
(\texttt{COHPstartEnergy}/\texttt{COHPendEnergy}) ; détection des
liaisons par distance interatomique de 0,1 à 6,0~\AA{}
(\texttt{cohpGenerator}, orbital par orbital) ; lissage gaussien de
0,05~eV (\texttt{gaussianSmearingWidth}).

\textbf{ICOHP/ICOBI intégrés (\texttt{ICOHPLIST.lobster}/
\texttt{ICOBILIST.lobster}).} L'ICOHP intégré d'une liaison est, par
définition LOBSTER, l'intégrale du COHP($E$) depuis le bas de la fenêtre
ci-dessus jusqu'au niveau de Fermi (états occupés uniquement), en eV ;
négatif = liant, positif = antiliant. L'ICOBI est la même intégrale sur
le COBI (indice d'ordre de liaison), sans dimension. Agrégation par
composé : \texttt{icohp\_sum}/
\texttt{icobi\_sum} sur toutes les étiquettes de liaison symétriquement
inéquivalentes de \texttt{ICOHPLIST.lobster}/\texttt{ICOBILIST.lobster}
(chaque liaison périodique comptée une fois, pas une fois par
direction) ; \texttt{icohp\_mean}/\texttt{icobi\_mean}, la moyenne
correspondante par liaison --- c'est cette dernière, intensive et donc
comparable entre composés de tailles de maille différentes, qui est
rapportée dans le Tableau~\ref{tab:bondtype} et dans les colonnes
\og{}ICOHP\fg{}/\og{}ICOBI\fg{} de l'Annexe~\ref{sec:appendix-master-list}.
Ces grandeurs alimentent \S\ref{sec:dataset} et les corrélations
classiques du \S\ref{sec:results}.

\textbf{$\Delta$(ICOHP) de réaction (\texttt{reaction\_analysis/}).} Pour
une réaction équilibrée (typiquement composé $\to$ éléments), $\Delta$ =
somme des ICOHP des produits $-$ somme des ICOHP des réactifs, en
convention \og{}produits $-$ réactifs\fg{}, en trois normalisations : par
unité formulaire (extensive), par atome transféré (la normalisation
utilisée dans tout ce rapport, sauf mention contraire), et par liaison
(diagnostique uniquement, non conservative --- les liaisons ne sont pas
conservées entre structures différemment liées). Signe positif
(\og{}endobondic\fg{}) : rompre les liaisons du réactif coûte plus que ce
que rapportent les liaisons des produits --- barrière cinétique
possible. Signe négatif (\og{}exobondic\fg{}) : aucune barrière visible
depuis la seule liaison.

\textbf{Population antiliante près du niveau de Fermi
(\texttt{cohp\_extraction.py}) --- grandeur distincte du $\Delta$(ICOHP)
ci-dessus.} Plutôt que d'intégrer tout le COHP occupé, cette grandeur
intègre uniquement sa partie antiliante (COHP$>0$) sur une fenêtre à
sens unique $(E_\text{réf}-\Delta E,\ E_\text{réf}]$, où
$E_\text{réf}=E_F$ (composés métalliques) ou le VBM (composés à gap), et
$\Delta E \in \{0.5, 1.0, 2.0\}$~eV ($\Delta E=1.0$~eV en fenêtre
principale) --- seuls les états occupés peuvent déstabiliser l'état
fondamental par antiliaison, d'où la fenêtre à sens unique plutôt que
symétrique autour de $E_\text{réf}$. Rapportée telle quelle (même
convention d'unité \og{}eV\fg{} que LOBSTER, bien qu'il s'agisse d'une
intégrale de population de Hamilton, pas littéralement d'une énergie) et
normalisée par la magnitude ICOHP totale occupée au même $E_\text{réf}$.
C'est cette grandeur, et elle seule, qui alimente le descripteur du
\S\ref{sec:antibonding} --- le $\Delta$(ICOHP) de réaction
(\S\ref{sec:reaction-icohp}--\ref{sec:reframing}) intègre tout le COHP
occupé sans restriction de fenêtre près de $E_F$.

\subsection{Statistiques}

Corrélation de Spearman (pas de raison d'attendre une relation linéaire)
entre chaque descripteur ICOHP/ICOBI et une cible de stabilité
(\texttt{energy\_above\_hull} ou \texttt{formation\_energy\_per\_atom}
selon le descripteur), globalement et par sous-groupe (\texttt{bond\_type},
\texttt{is\_metal}) ; tout sous-groupe $n<15$ est explicitement signalé,
jamais présenté nu. \textbf{Aucun SISSO, aucune régression symbolique} à
ce stade (voir \S\ref{sec:limites-actuelles}).

\section{Organigramme du workflow}

\begin{center}
\begin{tikzpicture}[
  node distance=0.75cm and 1.6cm,
  box/.style={draw, rounded corners, minimum width=6.2cm, minimum height=1.0cm,
              align=center, fill=blue!6, font=\small},
  io/.style={draw, minimum width=6.2cm, minimum height=1.0cm, align=center,
             fill=orange!10, font=\small},
  arr/.style={-{Latex[length=2.2mm]}, thick}
]
\node[io]  (mp)     {Materials Project (API) \\ 597 structures};
\node[box, below=of mp] (prep)   {Scripts \texttt{download\_*.py} + \texttt{prepare\_*vasp\_lobster.py} \\ POSCAR, INCAR, KPOINTS, POTCAR, lobsterin};
\node[box, below=of prep] (slurm)  {SLURM (\texttt{submit\_array.sh}) \\ tableau plafonné à 8 t\^aches concurrentes, HPC Yargla};
\node[box, below=of slurm] (vasplob) {VASP (statique, 16 c\oe urs) $\rightarrow$ LOBSTER 5.1.1 (16 threads) \\ ICOHPLIST.lobster, ICOBILIST.lobster};
\node[box, below=of vasplob] (desc)   {Extraction de descripteurs \\ \texttt{reaction\_icohp.py}, \texttt{cohp\_extraction.py}, \texttt{reaction\_analysis/}, \ldots};
\node[box, below=of desc] (join)   {\texttt{analysis/build\_dataset*.py} \\ jointure + descripteurs normalisés + bond\_type};
\node[io, below=of join] (stats)  {\texttt{analysis/stats\_analysis*.py} \\ corrélations, stratification, figures};

\draw[arr] (mp) -- (prep);
\draw[arr] (prep) -- (slurm);
\draw[arr] (slurm) -- (vasplob);
\draw[arr] (vasplob) -- (desc);
\draw[arr] (desc) -- (join);
\draw[arr] (join) -- (stats);
\end{tikzpicture}
\end{center}

\section{Environnement}

Identique sur l'ensemble des 597 structures (Yargla, VASP 6.5.0,
LOBSTER 5.1.1, \texttt{.venv} Python 3.11), complété par : \texttt{mp-api},
\texttt{pandas}, \texttt{scipy}, \texttt{scikit-learn}, \texttt{matplotlib},
\texttt{pydantic}, \texttt{pyyaml} (voir \texttt{requirements.txt}). Détail
des c\oe urs alloués par étape de calcul : \S\ref{sec:cout}.

\section{Jeu de données}
\label{sec:dataset}

L'ensemble des composés et éléments calculés compte \textbf{597
structures}.

\begin{table}[htbp]
\centering
\caption{Composition du jeu de données par nombre d'éléments.}
\label{tab:composition}
\begin{tabular}{@{}lr@{}}
\toprule
Composition & $n$ \\
\midrule
Références à un seul élément & 69 \\
Composés multi-éléments      & 522 \\
Structures de validation sans métadonnées de composé$^*$ & 6 \\
\midrule
\textbf{Total}                & \textbf{597} \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^*$ dimères gazeux isolés (H$_2$, N$_2$, O$_2$, F$_2$,
Cl$_2$) et CaO[sphalérite], utilisés uniquement pour la validation Reitz
\& Dronskowski (Annexe~\ref{sec:appendix-reitz-dronskowski-structures}),
hors du jeu de données principal.}
\end{table}

\begin{table}[htbp]
\centering
\caption{Classe de stabilité (par $E_\text{hull}$ et caractère expérimental/théorique).}
\label{tab:stability}
\begin{tabular}{@{}lrl@{}}
\toprule
Classe & $n$ & Plage $E_\text{hull}$ (eV/atome) \\
\midrule
stable ($E_\text{hull}=0$)  & 160 & 0.0000 \\
expérimentale métastable    & 183 & 0.0000347 -- 2.1080 \\
théorique métastable        & 242 & 0.0000843 -- 3.1688 \\
inconnue$^*$ & 12 & --- \\
\midrule
\textbf{Total}               & \textbf{597} & \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^*$ 2 références COD sans $E_\text{hull}$ (S$_4$N$_2$,
S$_4$N$_4$), 6 structures de validation sans métadonnées (voir
Table~\ref{tab:composition}), 1 référence COD supplémentaire
(ZnSn/NiAs), et 3 composés hérités de la campagne d'origine, catégorisés
\og{}metastable\fg{} avant l'introduction de l'indicateur
\texttt{theoretical}.}
\end{table}

\textbf{Type de liaison.} Deux classements indépendants existent (\S2.2) --- \texttt{classify()} et un classement fondé directement sur les données ICOHP/ICOBI --- présentés et confrontés à l'étape~3 de la section Résultats (\S\ref{sec:bondtype-classification}), pas ici : la classification n'est pas une fin en soi, elle sert uniquement à conditionner la corrélation viabilité-descripteur testée dans cette même section.

\section{Résultats}
\label{sec:results}

\subsection*{Logique de cette section}

Quatre étapes, dans cet ordre : (1) valider la méthodologie de ce projet
en reproduisant les 7 réactions de référence choisies par Reitz \&
Dronskowski ; (2) tester la corrélation centrale de ce projet ---
viabilité (stabilité thermodynamique/cinétique d'un composé) contre les
descripteurs issus des données LOBSTER (ICOHP, ICOHP antiliant, ICOBI,
ICOBI antiliant) ; (3) classifier les composés et éléments en
métallique/ionique/covalent/mixte directement à partir de ces mêmes
données ICOHP/ICOBI, et confronter ce classement à \texttt{classify()}
--- un outil au service de l'étape suivante, pas une fin en soi ; (4)
déterminer si la corrélation de l'étape 2 est renforcée ou atténuée une
fois conditionnée par le type de liaison de l'étape 3.

\subsection{Étape 1 --- Validation méthodologique : les 7 réactions de Reitz \& Dronskowski}
\label{sec:validation}

Détail complet : \texttt{analysis/REPORT\_delta\_icohp\_viability.md},
\texttt{analysis/test\_delta\_icohp\_viability.py}.

Les valeurs ICOHP par type de liaison de 7 réactions à $\Delta$ICOHP
indépendamment connu (transcrites indépendamment de tout calcul de ce
projet) sont passées de bout en bout par
\texttt{balance.py}/\texttt{delta.py}/\texttt{classify.py} :

\begin{table}[htbp]
\centering
\caption{Validation de la classification endobondic/exobondic sur 7 réactions de $\Delta$ICOHP indépendamment connu.}
\label{tab:reference-validation}
\begin{tabular}{@{}lrrll@{}}
\toprule
Réaction & Référence & Calculé & \multicolumn{2}{c}{Étiquette} \\
 & (kJ/mol) & (kJ/mol) & Réf. & Calculé \\
\midrule
Pb(N$_3$)$_2 \to$ Pb $+$ 3\,N$_2$ & $+1345$ & $+1344,3$ & endo & endo \\
S$_4$N$_2 \to \tfrac12$S$_8 + $N$_2$ & $+258$ & $+258,5$ & endo & endo \\
S$_4$N$_4 \to \tfrac12$S$_8 + 2$N$_2$ & $+399$ & $+397,3$ & endo & endo \\
ZnSn $\to$ Zn $+$ Sn & $-337$ & $-336,6$ & exo & exo \\
CaO[sphalérite] $\to$ CaO[rocksalt] & $-79$ & $-78,9$ & exo & exo \\
CaN $\to \tfrac13$Ca$_3$N$_2 + \tfrac16$N$_2$ & $-205$ & $-204,8$ & exo & exo \\
Mn$_2$O$_7 \to 2$MnO$_2 + \tfrac32$O$_2$ & $-186$ & $-187,7$ & exo & exo \\
\bottomrule
\end{tabular}
\end{table}

Les 7 cas reproduisent le $\Delta$ICOHP de référence à quelques kJ/mol
près (arrondi des valeurs par liaison de la source) et chaque
\texttt{BondingLabel} correspond. Le cas Mn$_2$O$_7$ est un garde-fou
utile en lui-même : exobondic selon ce critère de signe statique, alors
que Mn$_2$O$_7$ est un composé réel, observé, qui se décompose
simplement lentement (perte graduelle de O$_2$) plutôt que par rupture
brutale de liaison --- \texttt{classify.py} attache structurellement un
avertissement à \emph{chaque} verdict \texttt{UNSTABLE\_NONEXISTENT}
pour exactement cette raison, plutôt que de coder Mn$_2$O$_7$ en cas
particulier.

Cette validation ne compare que l'arithmétique des valeurs ICOHP
publiées, indépendamment de toute structure cristalline locale ---
l'Annexe~\ref{sec:appendix-reitz-dronskowski-structures} documente,
pour les 16 espèces impliquées, quelle structure MP/COD a été retenue
dans ce jeu de données et si elle a pu être vérifiée conforme à la
description du manuscrit (3 corrections réelles identifiées : N$_2$,
le polymorphe de Pb(N$_3$)$_2$, l'étain compétiteur de ZnSn). Pour N$_2$
et ZnSn, un calcul DFT+LOBSTER propre à ce projet (et non plus la seule
arithmétique du manuscrit) a en outre été comparé directement aux
valeurs du manuscrit (Tableau~\ref{tab:reitz-dronskowski-dft}) : N$_2$
reproduit l'ICOHP du manuscrit à 0,7\% près ; ZnSn reproduit le même
signe (décomposition favorable en énergie de liaison) mais avec une
amplitude $\sim$2,9$\times$ plus grande --- écart jugé non
significatif à ce stade, la somme d'ICOHP d'un intermétallique massif
étant beaucoup plus sensible aux choix de base/pseudopotentiel/coupure
qu'un simple dimère gazeux.
Reproduire la méthodologie DFT complète du manuscrit (PBEsol+D3, jeux de
points k du Tableau S1) a depuis été mené sur les 16 espèces
(2026-08-19) : 10/16 sont calculées et comparées directement
(Tableau~\ref{tab:reitz-dronskowski-dft}), les 6 restantes (Ca$_3$N$_2$,
Mn$_2$O$_7$, Pb(N$_3$)$_2$, S$_4$N$_2$, S$_4$N$_4$, S$_8$) étant encore
en calcul au moment de la rédaction (\S\ref{sec:limites-actuelles}).

La corrélation seule (Pearson/Spearman) n'est pas le bon test pour
\og{}nos chiffres correspondent-ils aux valeurs de référence\fg{} ---
elle est satisfaite par toute relation monotone, même avec un décalage
ou un facteur d'échelle systématique important. Le \textbf{coefficient
de corrélation de concordance de Lin} (CCC) teste spécifiquement
l'accord avec la droite d'identité (calculé = référence) :

\begin{table}[htbp]
\centering
\caption{Concordance calculé vs. référence sur les 7 réactions de validation.}
\label{tab:ccc}
\begin{tabular}{@{}lr@{}}
\toprule
Statistique & Valeur \\
\midrule
$n$ & 7 \\
CCC de Lin & \textbf{0,999998} \\
$r$ de Pearson & 0,999999 ($p=3,5\times10^{-15}$) \\
Accord de signe & 7/7 \\
$|$résidu$|$ moyen & 0,76 kJ/mol \\
$|$résidu$|$ max & 1,70 kJ/mol \\
RMSE & 0,98 kJ/mol \\
\bottomrule
\end{tabular}
\end{table}

Un CCC aussi proche de 1 signifie que les deux méthodes s'accordent
essentiellement dans la précision de rapport des valeurs de référence
elles-mêmes (arrondies à 2--3 décimales), pas seulement qu'elles
évoluent ensemble --- l'implémentation en est validée, pas seulement
vérifiée cas par cas. C'est cette implémentation, validée ici
indépendamment de tout calcul propre à ce projet, qui est appliquée à
l'ensemble du jeu de données à l'étape suivante.

\subsection{Étape 2 --- Viabilité versus descripteurs LOBSTER}
\label{sec:reframing}

\textbf{Agrégats ICOHP classiques (ligne de base).} Avant tout
descripteur construit, les agrégats ICOHP bruts (somme, minimum par
composé) sont testés contre $E_\text{hull}$ tels quels, sur le
sous-ensemble de 186 composés de la campagne d'origine :

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman, agrégats ICOHP classiques (somme, minimum) contre $E_\text{hull}$, sous-ensemble de 186 composés.}
\label{tab:classic-icohp}
\small
\begin{tabular}{@{}llrrr@{}}
\toprule
Groupe & Métrique & $n$ & $\rho$ & $p$ \\
\midrule
tous & ICOHP somme    & 186 & 0.032 & 0.670 \\
tous & ICOHP min      & 186 & 0.093 & 0.209 \\
métallique & ICOHP somme  & 88 & 0.223 & 0.037 \\
métallique & ICOHP min    & 88 & 0.198 & 0.065 \\
\bottomrule
\end{tabular}
\end{table}

Ces quatre lignes ne corrèlent pas ou peu avec $E_\text{hull}$, sauf
dans le sous-groupe métallique --- une ligne de base faible que les
descripteurs construits ci-dessous doivent dépasser.

\textbf{Population antiliante près du niveau de Fermi (ICOHP).}
\label{sec:antibonding}
Question distincte de l'ICOHP intégré en totalité (\S2.4) : non plus
\emph{combien} de liaison un composé possède, mais \emph{comment} le
COHP se répartit en énergie --- la fraction d'antiliaison occupée juste
sous le niveau de Fermi/VBM, par analogie avec les instabilités
électroniques de type Peierls/Jahn--Teller. Résultat, contre
\texttt{formation\_energy\_per\_atom} : $\rho=-0,155$,
$p=1,8\times10^{-4}$ ($n=579$) --- plus faible qu'à échelle réduite
($\rho=-0,282$, $n=346$) mais toujours réel et significatif ; le signe
reste \emph{inverse} de la lecture naïve Peierls/Jahn--Teller
(\S\ref{sec:stratification} pour la lecture stratifiée par type de
liaison, où le résultat le plus marquant de ce descripteur ne survit
plus à l'échelle actuelle).

\begin{center}
\includegraphics[width=0.75\textwidth]{antibonding_vs_formation_energy_by_bondtype.png}
\end{center}

\textbf{ICOHP de réaction.}
\label{sec:reaction-icohp}
Un analogue ICOHP de
\texttt{formation\_energy\_per\_atom} : l'ICOHP total d'un composé
est-il \og{}meilleur marché\fg{} que celui des mêmes atomes sous forme
d'éléments de référence ? Résultat : $\rho=0,295$, $p\approx0$
($n=510$) --- une corrélation réelle et hautement significative compte
tenu de la taille d'échantillon, avec un signe intuitif (moins de
liaison qu'à l'état élémentaire $\Rightarrow$ formation moins stable).
Sur un sous-ensemble de 288 composés partagé avec chaque autre
descripteur du projet, la population antiliante ci-dessus dépasse
légèrement l'ICOHP de réaction sur les deux cibles --- ce dernier reste
un descripteur fort, mais plus \emph{sans ambiguïté} le plus fort. Le
cas 2 (comparaison directe de polymorphes de même formule, 74 groupes) :
47,3\,\% d'accord entre le membre le plus liant et le plus stable,
contre une ligne de base au hasard de 42,4\,\% (test de
Poisson-binomiale exact, $P=0,22$) --- aucun signal.

\begin{center}
\includegraphics[width=0.75\textwidth]{reaction_icohp_vs_formation_energy_per_atom_by_bondtype.png}
\end{center}

\textbf{$\Delta$(ICOHP), appliqué à l'ensemble du jeu de données, poolé.}
\textbf{517 réactions de cas 1}, tout élément et composé ayant une
réaction de décomposition en éléments calculée, poolés en une seule
population (regrouper par origine de sélection serait exactement le
type de facteur de confusion entre groupes que la méthodologie de ce
projet est censée détecter) :

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman, $\Delta$ICOHP/atome (cas 1, 517 réactions) contre deux cibles de stabilité continues.}
\label{tab:pooled-targets}
\begin{tabular}{@{}lrrr@{}}
\toprule
Cible & $n$ & $\rho$ & $p$ \\
\midrule
\texttt{formation\_energy\_per\_atom} & 510 & $-0,2948$ & $1,1\times10^{-11}$ \\
\texttt{energy\_above\_hull\_eV\_per\_atom} & 514 & $-0,0564$ & $0,202$ \\
\bottomrule
\end{tabular}
\end{table}

(Signe : convention produits$-$réactifs de \texttt{reaction\_analysis},
opposée à la colonne propre de \texttt{reaction\_icohp.py} ci-dessus ---
même amplitude, signe inversé.) Les deux corrélations sont plus faibles
qu'à échelle réduite : le résultat sur l'énergie de formation était
$\rho=-0,50$ avant l'intégration des lots marginal-formation-energy et
max-hull-distance, et le résultat sur la distance au hull est passé de
limite ($p=0,068$) à clairement non significatif ($p=0,20$) --- cohérent
avec l'ajout délibéré de composés à thermodynamique marginale ou
extrême, qui diluent une corrélation de rang en partie portée par la
gamme de stabilité plus étroite de la population d'origine.

\textbf{Discrimination fondée sur le signe (endobondic/exobondic).}
Indicateur de viabilité expérimentale (\texttt{theoretical=False}
signifie réalisé expérimentalement) :

\begin{table}[htbp]
\centering
\caption{Étiquette endobondic/exobondic contre statut expérimental/théorique.}
\label{tab:fisher}
\begin{tabular}{@{}lrr@{}}
\toprule
 & endobondic & exobondic \\
\midrule
expérimental ($n=267$) & 79 & 188 \\
théorique seul ($n=244$) & 46 & 198 \\
\bottomrule
\end{tabular}
\end{table}

Test exact de Fisher : $p=0,0054$, rapport de cotes $=1,81$ ---
\textbf{significatif} (endobondic $\sim1,8\times$ plus fréquent parmi
les composés réalisés expérimentalement, le sens attendu). Mann--Whitney
sur le $\Delta$ICOHP/atome continu, même partition : médiane
expérimentale $-0,868$ eV, médiane théorique seule $-1,454$ eV (plus
exobondic), $p=0,0109$ --- même sens, également significatif. C'est
exactement le résultat que les lots marginal-formation-energy et
max-hull-distance ont été construits pour tester : la population poolée
à 281 réactions n'avait pas assez de composés à stabilité limite pour
révéler ce signal ($p=0,10$ à cette échelle).

\textbf{Attention, piège de lecture explicitement documenté par
\texttt{classify.py}} : la Table~\ref{tab:fisher} montre 188 composés
\emph{expérimentaux} (donc réellement existants) étiquetés
\emph{exobondic}. Ce n'est \textbf{pas} une incohérence --- exobondic
n'implique \og{}non viable\fg{} qu'après combinaison avec
\texttt{delta\_energy} (\texttt{classify\_viability()}) : un composé
n'est \texttt{UNSTABLE\_NONEXISTENT} que si la décomposition en
éléments est \emph{à la fois} exobondic \emph{et} thermodynamiquement
favorable ($\Delta E<0$), ce qui est rarissime (décomposer un composé
réel en éléments purs n'est presque jamais favorable). Vérifié
directement sur la même population que la Table~\ref{tab:fisher} :

\begin{table}[htbp]
\centering
\caption{\texttt{ViabilityLabel} calculé (\texttt{classify\_viability()}) sur la même population que la Table~\ref{tab:fisher} ($n=518$).}
\label{tab:viability-check}
\begin{tabular}{@{}lrr@{}}
\toprule
 & expérimental ($n=267$) & théorique ($n=244$) \\
\midrule
\texttt{STABLE\_ON\_HULL}        & 233 & 131 \\
\texttt{UNSTABLE\_NONEXISTENT}   & 26  & 95 \\
\texttt{METASTABLE\_VIABLE}      & 8   & 17 \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize 7 réactions supplémentaires (COD ou sans mp\_id, donc
sans \texttt{formation\_energy\_per\_atom}) exclues, données
insuffisantes.}
\end{table}

En regroupant \texttt{STABLE\_ON\_HULL}+\texttt{METASTABLE\_VIABLE}
(\og{}viable\fg{}) contre \texttt{UNSTABLE\_NONEXISTENT} : test exact de
Fisher $p=2,9\times10^{-15}$, rapport de cotes $\mathbf{6,0}$
($\chi^2=59,0$, $p=1,6\times10^{-14}$) --- \textbf{un effet bien plus
large que le test fondé sur le seul signe} (Table~\ref{tab:fisher},
$p=0,0054$, rapport de cotes 1,81). Un composé expérimentalement réalisé
a environ 6 fois moins de chances d'être prédit
\texttt{UNSTABLE\_NONEXISTENT} qu'un composé théorique seul. \textbf{Des
188 composés expérimentaux exobondic, 26 seulement sont prédits
\texttt{UNSTABLE\_NONEXISTENT}} --- les 162 autres sont
\texttt{STABLE\_ON\_HULL} : leur décomposition en éléments, bien
qu'exobondic, n'est pas thermodynamiquement favorable. Lire la
Table~\ref{tab:fisher} isolément comme \og{}exobondic = non viable\fg{}
donne donc une image trompeuse --- c'est la Table~\ref{tab:viability-check},
qui combine signe et thermodynamique, qui répond réellement à la
question de viabilité que l'indicateur \texttt{theoretical} sert de
proxy, et c'est de loin le résultat le plus fort de cette étape.

\begin{table}[htbp]
\centering
\caption{$\Delta$ICOHP/atome médian et \% endobondic par classe de stabilité ($n=177$).}
\label{tab:family-breakdown}
\begin{tabular}{@{}lrrr@{}}
\toprule
Classe & $n$ & médiane $\Delta$ICOHP/atome (eV) & \% endobondic \\
\midrule
expérimentale métastable & 59 & $-1,038$ & \textbf{28,8\%} \\
stable & 59 & $-1,391$ & 22,0\% \\
théorique métastable & 59 & $-1,601$ & \textbf{13,6\%} \\
\bottomrule
\end{tabular}
\end{table}

Kruskal--Wallis sur les trois groupes : $H=2,56$, $p=0,278$ ---
\textbf{non significatif} (population inchangée à $n=177$ : les lots
extension/marginal-formation-energy/max-hull portent un indicateur
\texttt{theoretical} mais pas encore d'étiquette \texttt{family}, donc
ce test précis n'a pas bénéficié de la croissance du jeu de données),
mais l'ordre reste exactement celui physiquement attendu : les composés
expérimentaux métastables ont la fraction endobondic la plus élevée ;
les composés théoriques métastables (jamais synthétisés) ont la plus
faible.

\textbf{Lecture d'étape.} Les 7 réactions de référence se reproduisent
presque exactement (étape 1) --- l'implémentation est correcte. Étendue
à l'ensemble du jeu de données actuel (517 réactions, contre 281
précédemment), la discrimination grossière fondée sur le seul signe est
significative, et la machinerie complète
\texttt{ViabilityLabel} --- signe combiné à la thermodynamique --- l'est
beaucoup plus fortement encore. Les corrélations continues contre
\texttt{formation\_energy\_per\_atom} et \texttt{energy\_above\_hull}
se sont affaiblies dans le même temps --- le prix attendu de l'ajout de
composés chimiquement divers et thermodynamiquement extrêmes à une
population auparavant plus étroite ; un test d'une nature différente
des résultats catégoriels ci-dessus, donc les deux ne sont pas en
tension. Reste à vérifier si le résultat \texttt{ViabilityLabel}
lui-même n'est pas confondu par le type de liaison (étape 4), ou par le
fait que les polymorphes théoriques du lot max-hull ont été
délibérément sélectionnés loin au-dessus du hull --- un effet de
sélection proche de \texttt{formation\_energy\_per\_atom}, pas
nécessairement une confirmation indépendante du mécanisme de barrière
cinétique.

\textbf{Toujours pas de SISSO à ce stade} : rapports détaillés,
\texttt{analysis/REPORT\_antibonding.md},
\texttt{analysis/REPORT\_reaction\_icohp.md}.

\subsection{Étape 3 --- Classification du type de liaison à partir des données ICOHP/ICOBI}
\label{sec:bondtype-classification}

Un outil au service de l'étape suivante, pas une fin en soi : cette
classification sert uniquement à tester, à l'étape 4, si la corrélation
viabilité-descripteur de l'étape 2 dépend du type de liaison d'un
composé. Deux classements indépendants (\S2.2) : la fonction
\texttt{classify()} (composition élémentaire + \texttt{is\_metal}) et un
classement fondé directement sur les données ICOHP/ICOBI de chaque
composé (\texttt{is\_metal} pour le caractère métallique, sinon l'ICOBI
de la paire d'espèces \textbf{dominante au premier voisinage} --- et non
une moyenne sur tous les voisins que LOBSTER rapporte dans un rayon de
coupure élargi, qui dilue fortement le signal des solides covalents
denses comme le diamant --- comparée à un seuil calibré au point médian
des médianes ICOBI des composés que \texttt{classify()} labellise déjà
ionique/covalent, soit 0,5315, pour distinguer ionique de covalent
parmi les composés non métalliques). Une quatrième catégorie,
\textbf{mixte} (type Zintl), identifie les composés où une paire
\emph{homoatomique} au-dessus du seuil covalent (p.~ex. N--N dans un
azoture, P--P dans un polyphosphure, S--S dans un polysulfure) coexiste
avec une paire \emph{hétéroatomique} nettement plus faible reliant un
cation distinct --- signature structurale de la chimie de Zintl-Klemm,
où un seul label ionique/covalent serait trompeur.

\begin{table}[htbp]
\centering
\caption{Type de liaison : \texttt{classify()} contre le classement ICOHP/ICOBI (concordance sur le sous-ensemble labellisé par \texttt{classify()} : 276/311 = 88,7\%).}
\label{tab:bondtype}
\begin{tabular}{@{}lrrrrr@{}}
\toprule
\texttt{classify()} $\backslash$ ICOHP/ICOBI & métallique & ionique & covalent & mixte & non classifié \\
\midrule
métallique     & 193 & 0  & 0  & 0 & 0 \\
ionique        & 0   & 48 & 2  & 3 & 0 \\
covalent       & 0   & 14 & 35 & 16 & 0 \\
non classifié  & 179 & 36 & 19 & 43 & 0 \\
\midrule
\textbf{Total} & \textbf{372} & \textbf{98} & \textbf{56} & \textbf{62} & \textbf{0} \\
\bottomrule
\end{tabular}
\end{table}

Sur les 277 composés que \texttt{classify()} laisse \og{}non
classifiés\fg{} (essentiellement les paires alcalin/alcalino-terreux
$\times$ \{N, P, S\}, hors de \texttt{IONIC\_ANIONS}, et les métaux
liés à un élément anion-like que \texttt{classify()} exclut
délibérément de sa catégorie \og{}métallique\fg{}), le classement
ICOHP/ICOBI en résout \textbf{55} en ionique (36) ou covalent (19), et
identifie \textbf{43} comme mixtes (type Zintl) ; les 179 restants sont
métalliques, déjà couverts par \texttt{is\_metal}. Sur le sous-ensemble
où les deux méthodes se prononcent (métallique/ionique/covalent),
l'accord est fort (88,7\%) mais pas total : 16 désaccords ionique/covalent
et 19 composés que \texttt{classify()} range en ionique ou covalent mais
que le classement ICOBI identifie comme mixtes (leur liaison
homoatomique dominante coexiste avec un lien hétéroatomique
nettement plus faible) --- à examiner cas par cas avant d'utiliser l'un
ou l'autre classement comme variable de stratification à l'étape 4.
C'est le classement ICOHP/ICOBI (plus couvrant, fondé sur les mêmes
données que les descripteurs de l'étape 2) qui est utilisé ci-dessous,
pas \texttt{classify()}.

\subsection{Étape 4 --- La corrélation viabilité-descripteur dépend-elle du type de liaison ?}
\label{sec:stratification}

\textbf{Population antiliante (ICOHP).} \textbf{Le résultat
précédemment le plus robuste du projet est rétracté} :
\texttt{bond\_type=covalent}, qui survivait nettement à $n=33$
($\rho=-0,551$, $p=0,0009$), ne survit plus une fois l'échantillon
monté à $n=54$ ($\rho=-0,046$, $p=0,74$) --- un mode d'échec déjà
rencontré ailleurs dans ce projet (un résultat de sous-groupe qui ne
se reproduit pas une fois l'échantillon agrandi), y compris pour ce
même descripteur sur son ancien sous-groupe \texttt{bond\_type=ionic}.
\textbf{Nouveau à cette échelle} :
\texttt{bond\_type=mixte} (Zintl) est la strate qui survit le
mieux ($\rho=-0,538$, $p=6,6\times10^{-6}$, $n=62$) ---
\textbf{renforcée} par rapport à la corrélation poolée de l'étape 2 ---
une catégorie qui n'existait quasiment pas au moment où le résultat
covalent avait été rapporté. \texttt{is\_metal=False} reste la strate
la plus constamment significative à travers les échelles successives de
ce projet ($\rho=-0,333$, $p=6,7\times10^{-7}$, $n=212$) ---
\textbf{renforcée}, pas affaiblie, par la croissance du jeu de données.
\texttt{is\_metal=True} a lui aussi convergé vers
\texttt{bond\_type=métallique} (même $n=367$, $\rho=0,056$, non
significatif), même phénomène que pour l'ICOHP de réaction ci-dessous.

\textbf{ICOHP de réaction.} La stratification \texttt{bond\_type}
(quatre catégories, \texttt{mixte}/Zintl incluse) ne dilue
plus uniformément la corrélation de l'étape 2 ($\rho=0,295$ poolée) :
\texttt{ionique} ($\rho=0,360$, $p=0,0004$) et \texttt{mixte}
($\rho=-0,525$, signe opposé, $p\approx0$) survivent nettement ---
\textbf{renforcées} en valeur absolue --- \texttt{covalent} aussi mais
change de signe ($\rho=-0,354$, $p=0,023$) ; seul \texttt{métallique}
(la classe majoritaire, 315/510 lignes) reste non significatif ---
\textbf{atténuée} jusqu'à disparaître dans cette strate. \textbf{La
stratification \texttt{is\_metal} --- celle qui survivait auparavant ---
converge vers la même population que
\texttt{bond\_type=métallique}} (même $n$, même $\rho$, même $p$ sur
chaque ligne) et hérite donc de son résultat non significatif : c'est
\texttt{bond\_type}, et non plus \texttt{is\_metal}, qui porte
l'information à la composition actuelle du jeu de données. Contre
\texttt{energy\_above\_hull}, \texttt{bond\_type=mixte} survit à nouveau
seul ($\rho=-0,307$, $p=0,016$).

\begin{center}
\includegraphics[width=0.75\textwidth]{reaction_icohp_vs_formation_energy_per_atom_by_bondtype.png}
\end{center}
\begin{center}
\includegraphics[width=0.75\textwidth]{antibonding_vs_formation_energy_by_bondtype.png}
\end{center}

\textbf{Synthèse.} Dans les deux cas (antiliaison et ICOHP de réaction),
la réponse à la question de cette étape est \emph{oui, mais pas
uniformément} : le type de liaison ne se contente pas d'affaiblir ou de
renforcer une seule corrélation globale, il change le \emph{signe même}
de la relation selon la strate (ionique positif, covalent/mixte négatif
pour l'ICOHP de réaction) --- le signal utile est conditionnel
au type de liaison. La rétractation du résultat covalent de la
population antiliante est elle-même une illustration directe de la
raison d'être de cette étape 4 : un chiffre stratifié n'est digne de
confiance qu'une fois revérifié à mesure que le jeu de données grandit,
pas accepté une fois pour toutes. Une recherche de caractéristiques
(SISSO) poolée serait donc activement trompeuse ici ; toute recherche
future devrait être menée séparément par \texttt{bond\_type}, pas sur la
population poolée.

\section{Incidents résolus}
\label{sec:incidents}

Conformément à la consigne (\og{}chaque échec de job doit être loggé avec la
raison probable\fg{}), 5 des 178 premiers jobs soumis en tableau SLURM ont
échoué au premier passage, tous diagnostiqués et corrigés à la source
(les correctifs, portés sur les gabarits SLURM/INCAR partagés,
s'appliquent à l'ensemble du jeu de données) :

\begin{itemize}
  \item \textbf{Al$_{12}$W, BW, WO$_2$, TcW, TiW} (tous contenant du
    tungstène) :
    le POTCAR \texttt{W\_sv} du miroir local est dépourvu de son champ
    \texttt{LEXCH} (confirmé par inspection directe --- présent pour toutes
    les autres variantes testées, y compris \texttt{W} simple), ce que VASP
    interprète comme une fonctionnelle d'échange-corrélation incompatible
    et refuse d'exécuter. Corrigé en remplaçant la substitution
    \texttt{W\_sv} par \texttt{W} (PBE valide, vérifié).
  \item \textbf{Al$_{12}$W} a échoué une seconde fois après cette correction,
    avec un SIGSEGV VASP --- un problème connu
    (\texttt{ulimit -s unlimited} avant \texttt{vasp\_std}), présent
    dans \texttt{submit\_array.sh} mais oublié dans le gabarit SLURM
    individuel de \texttt{prepare\_vasp\_lobster.py}. Corrigé ; récupéré au
    second essai.
\end{itemize}

Taux de succès final sur l'ensemble des 597 structures calculées à ce
jour : \textbf{597/597} pour VASP ; \textbf{596/597} pour LOBSTER (1
référence à maille triviale sans étape LOBSTER exploitable, \S\ref{sec:cout}).

\section{Coût de calcul}
\label{sec:cout}

\textbf{597 structures calculées au total} : 69 références à un seul
élément, 522 composés multi-éléments, et 6 structures de validation
supplémentaires sans métadonnées de composé mais avec leur propre calcul
VASP+LOBSTER (\S\ref{sec:dataset}).

Chaque job VASP puis LOBSTER s'exécute sur le HPC \textbf{Yargla}, au sein
d'une même allocation SLURM d'un n\oe ud (\texttt{--nodes=1}) et
\textbf{16 c\oe urs} : VASP en parallélisme MPI
(\texttt{--ntasks=16 --cpus-per-task=1}, \texttt{srun}), puis LOBSTER en
parallélisme OpenMP sur les mêmes 16 c\oe urs
(\texttt{OMP\_NUM\_THREADS=16}, processus unique, exécuté séquentiellement
après VASP dans le même job). Le tableau SLURM d'origine
(\texttt{submit\_array.sh}, campagne principale) est plafonné à 8 tâches
concurrentes ; les lots ajoutés depuis (extension/marginal-formation-energy/
max-hull/widen) ont été soumis individuellement (\texttt{sbatch} par
répertoire), sans ce plafond propre mais en partageant le cluster avec
d'autres charges de travail non liées au projet.

\begin{table}[htbp]
\centering
\caption{Temps de calcul VASP et LOBSTER (597 structures).}
\label{tab:compute-cost}
\begin{tabular}{@{}lrr@{}}
\toprule
 & VASP & LOBSTER \\
\midrule
$n$ & 597 & 596$^*$ \\
Moyenne (s) & 188 & 578 \\
Médiane (s) & 72 & 157 \\
Maximum (s) & 5715 (1.6 h) & 22555 (6.3 h) \\
Total séquentiel & 31.2 h & 95.8 h \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^*$ 1 structure sur 597 sans étape LOBSTER exploitable
(élément de référence à maille triviale).}
\end{table}

Total séquentiel combiné (VASP + LOBSTER, 597 structures) :
\textbf{127.0 h}. La campagne principale (plafonnée à 8 tâches
concurrentes) donnait un ordre de grandeur de temps réel écoulé
d'environ un huitième du total séquentiel ; ce facteur ne s'applique
plus uniformément à l'ensemble --- les lots soumis individuellement
depuis n'ont pas ce plafond propre mais ont partagé la file d'attente du
cluster avec d'autres projets, ce qui rend une estimation de temps réel
écoulé peu significative à l'échelle du jeu de données complet.

\section{Limites et perspectives actuelles}
\label{sec:limites-actuelles}

\begin{itemize}
  \item \textbf{Un composé exclu pour qualité LOBSTER} :
    \texttt{extension\_Cu4Au\_mp-1225761} a été retiré de toutes les
    analyses (mais pas du jeu de données brut, calcul VASP+LOBSTER
    conservé pour référence) --- \texttt{lobsterout} rapporte un échec
    d'orthonormalisation sur 343/343 points k (100\%, pas un cas
    limite), et \texttt{bandOverlaps.lobster} affiche une déviation
    maximale de 11,4 (le seuil d'alerte déjà établi pour ce projet est
    0,1). Conséquence directe : des valeurs ICOHP physiquement
    implausibles (contacts Cu--Cu au 3e voisin à 4,25--5,04~\AA{}
    avec des ICOHP de 7,6 à 11,6~eV) et un $\Delta$ICOHP/atome de
    $-380,5$~eV, $\sim$27$\times$ le cas le plus extrême du reste de la
    population. Un balayage systématique de l'ensemble des 597
    structures (\texttt{analysis/audit\_lobster\_quality.py}, critère :
    échec à 100\% des points k \emph{et} déviation $>0,5$ \emph{et}
    score-z robuste $>10$) confirme qu'il s'agit d'un cas unique, pas
    d'un problème plus large --- le second cas le plus extrême
    (\texttt{Ir4W}) est déjà 29$\times$ moins extrême et plausiblement
    une chimie réelle (intermétallique dense riche en électrons~d).
    Toutes les statistiques de ce rapport portant sur les 518 réactions
    de cas~1 ont été régénérées sur les 517 réactions restantes ;
    l'effet sur chaque nombre est négligeable (3e--4e
    décimale), aucune conclusion ni seuil de significativité n'est
    affecté.
  \item \textbf{Couverture de \texttt{classify()}} : 277/588 composés
    (47\%) hors des trois catégories ionique/covalent/métallique,
    principalement les liaisons alcalin/alcalino-terreux $\times$
    \{N, P, S\} qu'\texttt{IONIC\_ANIONS} ne reconnaît pas
    (\S\ref{sec:dataset}) --- purement informatif ici, mais une vraie
    limite pour toute analyse qui voudrait s'appuyer dessus pour
    stratifier.
  \item \textbf{Pas de classe \og{}instable\fg{} au sens dynamique} :
    \texttt{theo\_metastable} est un proxy (composés jamais synthétisés),
    pas un résultat de calcul de phonons.
  \item \textbf{Taille d'échantillon par sous-groupe} : reste la
    limite structurelle de ce projet à chaque nouvelle stratification
    (\texttt{bond\_type}, \texttt{is\_metal}) --- voir
    \S\ref{sec:antibonding} pour un exemple concret où un sous-groupe
    $n<15$ ne s'est pas reproduit une fois l'échantillon agrandi.
  \item \textbf{Pourquoi pas SISSO maintenant} : le signal de l'ICOHP de
    réaction (\S\ref{sec:reaction-icohp}) est conditionnel au
    type de liaison et de signe opposé selon les strates (ionique
    positif, covalent/mixte négatif) --- une recherche poolée serait
    trompeuse ; toute recherche future devrait être menée séparément par
    \texttt{bond\_type}.
  \item \textbf{\texttt{family} non étendu aux nouveaux lots} : la
    discrimination \texttt{ViabilityLabel}/Fisher fondée sur
    \texttt{theoretical} (\S\ref{sec:reframing}) bénéficie pleinement de
    la croissance du jeu de données, mais la partition plus fine à trois
    classes (\texttt{exp\_metastable}/\texttt{exp\_stable}/
    \texttt{theo\_metastable}) reste à $n=177$ faute d'étiquette
    \texttt{family} sur les lots extension/marginal-formation-energy/
    max-hull --- prochaine étape directe pour tester si cette partition
    plus fine devient elle aussi significative.
  \item \textbf{Le résultat \texttt{ViabilityLabel} (§\ref{sec:reframing},
    $p=2,9\times10^{-15}$) n'a pas encore été vérifié pour un facteur de
    confusion} : ni stratifié par \texttt{bond\_type}/\texttt{is\_metal},
    ni distingué d'un effet de sélection (les polymorphes théoriques du
    lot max-hull ont été délibérément choisis loin au-dessus du hull).
  \item \textbf{Comparaison DFT+LOBSTER de validation-manuscrit
    (Annexe~\ref{sec:appendix-reitz-dronskowski-structures}), 6/16
    espèces encore en calcul} : Ca$_3$N$_2$, Mn$_2$O$_7$,
    Pb(N$_3$)$_2$, S$_4$N$_2$, S$_4$N$_4$, S$_8$ étaient encore sur
    SLURM au moment de la rédaction (2026-08-19). Sur les 10 espèces
    déjà comparées, trois (Pb, MnO$_2$, O$_2$) présentent des résidus
    ouverts de $\sim$2--10\% sous la méthodologie du manuscrit
    (PBEsol+D3) comme sous celle par défaut de ce projet (PBE) à la
    fois --- examinés et écartés comme instances du bug de détection de
    sphère du Zn, laissés comme questions de méthodologie probables non
    résolues (couplage spin-orbite pour Pb ; traitement AFM/Hubbard-U et
    polarisation de spin pour MnO$_2$/O$_2$) plutôt que forcés à zéro.
  \item \textbf{La question de savoir si le bug de détection de sphère
    du Zn (Annexe~\ref{sec:appendix-reitz-dronskowski-structures})
    affecte la classification \texttt{bond\_type} d'autres composés
    reste ouverte} : deux balayages heuristiques rapides des spectres
    de distance de première sphère du jeu de données complet
    (2026-08-19) ont chacun signalé 40--47\% des groupes de paires sous
    des seuils trop lâches, dominés par la structure fine normale
    intra-sphère et des distances quasi dupliquées en virgule flottante,
    pas par de vraies doubles sphères quasi dégénérées à la Zn ---
    non concluant par construction, pas une preuve d'un problème
    répandu. Un audit calibré (nécessitant une vérité terrain par
    composé, qui n'existe que pour les 16 espèces du manuscrit) reste à
    faire avant de trancher dans un sens ou l'autre.
\end{itemize}

\section{Conclusion}

La méthodologie endobondic/exobondic de Reitz \& Dronskowski
(\S\ref{sec:reframing}) se reproduit fidèlement sur les 7 réactions de
référence indépendamment connues (CCC de Lin 0,999998) et s'applique à
l'ensemble du jeu de données de ce rapport (517 réactions de
décomposition en éléments). Sur cette population, la discrimination
fondée sur le seul signe du $\Delta$ICOHP est significative
(test de Fisher, $p=0,0054$) et la machinerie complète
\texttt{ViabilityLabel} --- signe combiné à la thermodynamique --- l'est
beaucoup plus fortement encore ($p=2,9\times10^{-15}$, rapport de cotes
6,0) : un composé expérimentalement réalisé a environ 6 fois moins de
chances d'être prédit \texttt{UNSTABLE\_NONEXISTENT} qu'un composé
théorique seul (\S\ref{sec:reframing}). L'ICOHP de réaction reste, par
ailleurs, un corrélat continu fort du jeu de données contre l'énergie de
formation ($\rho=0,295$, \S\ref{sec:reaction-icohp}), bien que la
population antiliante le dépasse légèrement sur le
sous-ensemble partagé par les deux descripteurs.

Deux directions concrètes se dégagent. D'une part, vérifier si le
résultat \texttt{ViabilityLabel} ci-dessus résiste à une stratification
par \texttt{bond\_type}/\texttt{is\_metal} et n'est pas simplement porté
par l'effet de sélection délibéré du lot max-hull --- la même
discipline de vérification que ce projet applique systématiquement à
chaque nouveau descripteur. D'autre part, restreindre le $\Delta$(ICOHP) de
réaction à une fenêtre proche du niveau de Fermi plutôt que sur l'ICOHP
intégré en totalité, sur le modèle de la population antiliante du
\S\ref{sec:antibonding} : deux variantes de cette construction sont
détaillées dans l'Annexe~\ref{sec:appendix-icobi-antibonding}
(sous-liaison antiliante seule, et ICOHP signé complet restreint à la
fenêtre) --- la première survit à l'intégralité des stratifications
testées, un résultat qu'aucun autre descripteur de ce rapport n'atteint.

\vspace{1cm}
\noindent\textit{Code source :} \url{https://github.com/John-Akapulco/viability} \\
\noindent\textit{Données :} \texttt{analysis/} (jeux de données et scripts de ce rapport) \\
\noindent\textit{Rapports détaillés :} \texttt{analysis/REPORT\_*.md}

\appendix
\renewcommand{\thetable}{S\arabic{table}}
\setcounter{table}{0}

\begin{center}
\Large\textbf{Annexe (SI)}
\end{center}
\vspace{0.3cm}

\section{Liste des composés et éléments}
\label{sec:appendix-master-list}

Liste complète des 392 composés et éléments disposant d'une donnée ICOBI
exploitable (sur 394 au total ; 2 composés d'origine COD sans
\texttt{is\_metal}/ICOBI, \S\ref{sec:dataset}), organisée par section
selon le classement ICOHP/ICOBI indépendant du \S\ref{sec:dataset}
(\texttt{is\_metal} pour le caractère métallique ; sinon l'ICOBI de la
paire d'espèces \textbf{dominante au premier voisinage} (pas une
moyenne sur tous les voisins LOBSTER dans le rayon de coupure élargi
--- voir \S2.4) comparée à un seuil de 0,5052 pour distinguer ionique
de covalent ; une catégorie \og{}mixte\fg{} (type Zintl) regroupe les
composés où une paire homoatomique fortement covalente coexiste avec
une paire hétéroatomique nettement plus faible, p.~ex. l'ion azoture
N$_3^-$ lié ioniquement à son cation) --- pas de colonne \og{}type de
liaison\fg{}, chaque section indique sa propre plage d'ICOBI observée
à la place. ICOBI et ICOHP sont des
moyennes par liaison (\S2.4) ; les populations antiliantes ICOHP/ICOBI
sont les valeurs normalisées à $\Delta E=1{,}0$~eV
(\S\ref{sec:antibonding}, \S\ref{sec:appendix-icobi-antibonding}).

\subsection{Métalliques}

{\small
\input{appendix_master_list_metallic_fr.tex}
}

\subsection{Ioniques}

{\small
\input{appendix_master_list_ionic_fr.tex}
}

\subsection{Covalents}

{\small
\input{appendix_master_list_covalent_fr.tex}
}

\subsection{Mixtes (type Zintl)}

{\small
\input{appendix_master_list_mixed_fr.tex}
}

\subsection{Non classifiés}

{\small
\input{appendix_master_list_unclassified_fr.tex}
}

\section{Paramètres de calcul VASP}
\label{sec:appendix-incar}

Paramètres INCAR fixes, identiques sur l'ensemble du jeu de données
(calcul statique, compatible LOBSTER) --- seuls \texttt{NBANDS}
(annexe~\ref{sec:appendix-kpoints}) et la grille de points k varient d'un
composé à l'autre.

\begin{center}
\input{appendix_dft_common_fr.tex}
\end{center}

\section{Potentiels PAW-PBE}

Un potentiel PAW-PBE par élément présent dans le jeu de données
(bibliothèque \texttt{MPRelaxSet} de \texttt{pymatgen} --- la même que
celle utilisée par Materials Project pour relaxer les structures fournies
en entrée), à l'exception documentée du tungstène (\S\ref{sec:incidents}) : la variante
\texttt{W\_pv} recommandée est absente du miroir local, et
\texttt{W\_sv} s'est révélée corrompue (champ \texttt{LEXCH} manquant) ---
le potentiel \texttt{W} simple est utilisé à la place, vérifié valide.

{\small
\input{appendix_dft_potcar_fr.tex}
}

\section{Grille de points k et NBANDS par composé}
\label{sec:appendix-kpoints}

Grille de points k générée automatiquement par
\texttt{pymatgen.io.vasp.Kpoints.automatic\_density\_by\_vol} (densité
\texttt{kppvol=100}, maillage $\Gamma$ ou Monkhorst--Pack selon la symétrie
de la maille) et \texttt{NBANDS}, dérivé par composé du nombre d'orbitales
locales de la base LOBSTER (\texttt{Lobsterin.write\_INCAR}) --- valeur
strictement suffisante pour la projection sur la base locale, pas une
estimation fixe.

{\small
\input{appendix_dft_kpoints_fr.tex}
}

\section{Éléments et allotropes}
\label{sec:appendix-ext-elements}

Chaque structure à un seul élément sans polymorphe apparié dans le jeu
de données : les références élémentaires (\texttt{ELEMENT\_REFERENCE})
utilisées pour décomposer les composés multi-éléments. Un composé
expérimental porte une étoile en exposant sur sa formule (noire si
c'est la phase stable, rouge si c'est un polymorphe/allotrope
métastable) ; une formule non marquée est théorique. Généré
automatiquement par \texttt{report/gen\_appendix\_extension.py}.

{\small
\input{appendix_extension_elements_fr.tex}
}

\section{Polymorphes et allotropes}
\label{sec:appendix-ext-polymorphs}

Éléments et composés ayant plusieurs entrées de même formule dans le
jeu de données, regroupés ici plutôt que dispersés dans les tableaux
d'éléments/composés (voir la légende du tableau pour le détail).

{\small
\input{appendix_extension_polymorphs_fr.tex}
}

\section{Composés cibles}
\label{sec:appendix-ext-compounds}

Chaque composé cible multi-élément sans polymorphe apparié dans le jeu
de données, avec le $\Delta$ICOHP par atome et son étiquette
endobondic/exobondic lorsqu'une réaction de décomposition de cas 1 a pu
être calculée. Généré automatiquement depuis
\texttt{analysis/reaction\_icohp\_case1.csv} et
\texttt{analysis/reaction\_analysis\_case1\_full.csv} par
\texttt{report/gen\_appendix\_extension.py}.

{\small
\input{appendix_extension_compounds_fr.tex}
}

\section{Réactions endobondic}
\label{sec:appendix-ext-endobondic}

Le sous-ensemble des réactions de décomposition de cas 1
($A_aB_b \to a\,A + b\,B$, références élémentaires à l'état standard,
équilibrées par \texttt{reaction\_icohp.py}) étiquetées
\textbf{endobondic} ($\Delta$ICOHP $\geq 0$, convention produits $-$
réactifs --- signe \emph{opposé} à la colonne
\texttt{delta\_icohp\_per\_atom} propre à \texttt{reaction\_icohp.py}
rapportée ailleurs dans ce projet ; voir la docstring de
\texttt{delta.py}).

{\small
\input{appendix_extension_endobondic_fr.tex}
}

\section{Réactions exobondic}
\label{sec:appendix-ext-exobondic}

Le sous-ensemble complémentaire \textbf{exobondic} ($\Delta$ICOHP $< 0$),
même convention et provenance que l'Annexe~\ref{sec:appendix-ext-endobondic}.

{\small
\input{appendix_extension_exobondic_fr.tex}
}

\section{Descripteurs antiliants ICOHP et ICOBI}
\label{sec:appendix-icobi-antibonding}

\subsection{Implémentation}

Le descripteur ICOHP de population antiliante près du niveau de Fermi
(\S\ref{sec:antibonding}, \texttt{cohp\_extraction.py}) est étendu ici à
l'ICOBI : même fenêtre à sens unique
$(E_\text{réf}-\Delta E,\ E_\text{réf}]$, même $E_\text{réf}$ (niveau de
Fermi pour les métaux, VBM pour les composés à gap --- logique
totalement indépendante de la grandeur intégrée), lue cette fois dans
\texttt{COBICAR.lobster}/\texttt{ICOBILIST.lobster} plutôt que dans
\texttt{COHPCAR.lobster}/\texttt{ICOHPLIST.lobster}
(\texttt{pymatgen.io.lobster.outputs.Cohpcar(are\_cobis=True)}).

\textbf{Point critique, vérifié empiriquement et non supposé} : l'ICOBI
\textbf{n'a pas} la convention de signe de l'ICOHP. Alors que
\og{}ICOHP négatif = liant\fg{} (\S2.4), l'ICOBI suit la convention
inverse, \og{}ICOBI positif = liant\fg{} (comme le COOP, pas comme le
COHP). Vérifié deux fois sur données réelles : (1) recoupement exact
avec \texttt{ICOBILIST.lobster} (écart maximal 0,0 sur 66 étiquettes de
liaison, \texttt{tests/test\_cohp\_extraction.py}) --- \texttt{pymatgen}
ne renormalise pas silencieusement le signe en passant de COHP à COBI ;
(2) sur la liaison Si--O la plus forte d'\texttt{extension\_SiO2\_anchor}
(ICOHP $=-5{,}599$, ICOBI $=+0{,}513$), la région profonde de liaison
($-10$ à $-3$~eV, loin de toute perturbation de frontière) donne un COHP($E$)
moyen de $-0{,}338$ mais un COBI($E$) moyen de $+0{,}041$ --- signes
opposés dans la même région physiquement liante. La partie antiliante de
l'ICOBI est donc sa partie \emph{négative}, pas positive --- le sens de
troncature (\texttt{integrate\_icobi\_antibonding\_in\_window()}) est
délibérément le miroir de celui utilisé pour l'ICOHP, pas une copie.

Calculé sur les 588 composés disposant d'un \texttt{COBICAR.lobster} et
d'un \texttt{COHPCAR.lobster}, \texttt{is\_metal} disponible pour la
totalité (\texttt{analysis/compute\_icobi\_antibonding\_all.py},
\texttt{analysis/compute\_antibonding\_all\_full.py} --- un composé,
\texttt{Cu4Au}, exclu pour échec catastrophique de la projection LOBSTER,
\S\ref{sec:limites-actuelles}).

\subsection{Corrélation à l'énergie de formation}

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman (normalisées, $\Delta E=1{,}0$~eV) contre \texttt{formation\_energy\_per\_atom} --- ICOHP et ICOBI antiliants, même population ($n=588$).}
\label{tab:antibonding-icohp-icobi}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Groupe & $n$ (ICOHP) & $\rho$ (ICOHP) & $n$ (ICOBI) & $\rho$ (ICOBI) \\
\midrule
tous                & 579 & $-0{,}1550^{***}$ & 579 & $+0{,}2308^{***}$ \\
\texttt{covalent}   & 54  & $-0{,}0464$       & 54  & $+0{,}1129$ \\
\texttt{ionique}    & 96  & $-0{,}1958$       & 96  & $+0{,}1615$ \\
\texttt{métallique} & 367 & $+0{,}0563$       & 367 & $+0{,}1197^{*}$ \\
\texttt{mixte}      & 62  & $-0{,}5376^{***}$ & 62  & $+0{,}0251$ \\
\texttt{is\_metal=False} & 212 & $-0{,}3334^{***}$ & 212 & $-0{,}0108$ \\
\texttt{is\_metal=True}  & 367 & $+0{,}0563$       & 367 & $+0{,}1197^{*}$ \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^{*}p<0{,}05$ ; $^{***}p<0{,}001$ ; aucune étoile =
non significatif. Détail complet (brut + normalisé, les trois
$\Delta E$) : \texttt{analysis/stats\_summary\_antibonding\_joint.json}.}
\end{table}

\begin{center}
\includegraphics[width=\textwidth]{icohp_vs_icobi_antibonding_comparison.png}
\end{center}

\textbf{Les deux descripteurs se comportent toujours différemment, et
\emph{aucun} des deux ne conserve son ancien résultat stratifié
vedette.} Le signe global de l'ICOBI antiliant reste \emph{opposé} à
celui de l'ICOHP antiliant ($+0{,}23$ contre $-0{,}16$) --- pour
l'ICOHP, plus d'antiliaison occupée près de $E_F$ est associé à une
formation \emph{plus} stable (déjà noté comme contre-intuitif au
\S\ref{sec:antibonding}) ; pour l'ICOBI, la relation va dans le sens
naïvement attendu. \textbf{Le sous-groupe covalent de l'ICOHP
antiliant, longtemps le résultat stratifié le plus robuste du projet
($\rho=-0{,}551$ à $n=33$), ne survit plus à $n=54$
($\rho=-0{,}046$, $p=0{,}74$)} --- rétracté, \S\ref{sec:antibonding}.
\textbf{Nouveau à cette échelle} : \texttt{bond\_type=mixte} (Zintl)
domine nettement pour l'ICOHP antiliant ($\rho=-0{,}538$,
$p\approx0$), une catégorie qui n'existait quasiment pas quand le
résultat covalent avait été établi ; \texttt{is\_metal=False} reste la
strate la plus constamment significative du descripteur ICOHP à travers
les échelles successives de ce projet. Côté ICOBI, seul
\texttt{métallique} atteint une significativité (faible, $p=0{,}022$) ;
aucun autre sous-groupe ICOBI ne survit, comme précédemment. La
corrélation globale de l'ICOBI reste donc, comme pour l'ICOHP de
réaction (\S\ref{sec:reaction-icohp}), vraisemblablement un effet de
regroupement entre types de liaison plutôt qu'une relation robuste au
sein d'un groupe homogène.

\subsection{$\Delta$(antiliant) de la réaction de décomposition en éléments}

Pour une réaction de cas 1 (composé $\to$ éléments), le
$\Delta$(antiliant) n'est \textbf{pas} calculé par
\texttt{reaction\_analysis/delta.py} (qui combine des sommes ICOHP/ICOBI
\emph{extensives} par unité formulaire avec des coefficients entiers) :
la population antiliante près de la frontière est construite à partir de
la trace \og{}average\fg{} de LOBSTER, une grandeur \emph{intensive}
(moyennée sur toutes les liaisons, pas sommée) qui n'a pas de
décomposition extensive naturelle. Convention choisie ici, documentée
comme un choix méthodologique explicite plutôt qu'une réutilisation
silencieuse : $\Delta$ = (moyenne des descripteurs antiliants des
éléments, pondérée par fraction atomique) $-$ (descripteur antiliant
propre du composé), toujours en convention produits $-$ réactifs.
Calculé pour 439 réactions de cas 1
(\texttt{analysis/compute\_delta\_antibonding\_case1.py}).

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman, $\Delta$(antiliant) de la réaction de décomposition (cas 1, $n=434$--439).}
\label{tab:delta-antibonding}
\begin{tabular}{@{}lrrr@{}}
\toprule
Descripteur & Cible & $n$ & $\rho$ \\
\midrule
$\Delta$(ICOHP antiliant) & \texttt{formation\_energy\_per\_atom} & 434 & $-0{,}6175^{***}$ \\
$\Delta$(ICOBI antiliant) & \texttt{formation\_energy\_per\_atom} & 434 & $-0{,}4270^{***}$ \\
$\Delta$(ICOHP antiliant) & \texttt{energy\_above\_hull} & 438 & $-0{,}1734^{***}$ \\
$\Delta$(ICOBI antiliant) & \texttt{energy\_above\_hull} & 438 & $-0{,}1215^{*}$ \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^{*}p<0{,}05$ ; $^{***}p<0{,}001$.}
\end{table}

\textbf{$\Delta$(ICOHP antiliant) reste, à cette échelle, la corrélation
continue la plus forte de tout le projet} ($\rho=-0{,}618$, plus forte
que l'ICOHP de réaction du \S\ref{sec:reaction-icohp}, $\rho=0{,}295$) ---
affaiblie par rapport au $\rho=-0{,}644$ rapporté à échelle réduite,
mais toujours la plus forte. Fait notable, inchangé : contrairement aux
valeurs brutes (signes opposés, ci-dessus), \textbf{les deux $\Delta$
vont dans le même sens} (tous deux négatifs). Les deux $\Delta$ sont
eux-mêmes corrélés entre eux ($\rho=0{,}588$, $p<0{,}001$, $n=439$) :
liés mais pas redondants.

\subsection{Stratification par type de liaison}

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman, $\Delta$(antiliant) contre \texttt{formation\_energy\_per\_atom}, par sous-groupe (cas 1, $n=286$--$434$).}
\label{tab:delta-antibonding-stratified}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Sous-groupe & $n$ & $\rho$ ($\Delta$ICOHP) & $n$ & $\rho$ ($\Delta$ICOBI) \\
\midrule
tous                & 434 & $-0{,}6175^{***}$ & 434 & $-0{,}4270^{***}$ \\
\texttt{covalent}   & 29  & $-0{,}6956^{***}$ & 29  & $-0{,}7750^{***}$ \\
\texttt{ionique}    & 87  & $-0{,}6337^{***}$ & 87  & $-0{,}2245^{*}$ \\
\texttt{métallique} & 286 & $-0{,}4316^{***}$ & 286 & $-0{,}3550^{***}$ \\
\texttt{mixte}      & 32  & $-0{,}6394^{***}$ & 32  & $-0{,}8397^{***}$ \\
\texttt{is\_metal=False} & 148 & $-0{,}6459^{***}$ & 148 & $-0{,}4131^{***}$ \\
\texttt{is\_metal=True}  & 286 & $-0{,}4316^{***}$ & 286 & $-0{,}3550^{***}$ \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^{*}p<0{,}05$ ; $^{***}p<0{,}001$ ; aucune étoile = non
significatif.}
\end{table}

\textbf{$\Delta$(ICOHP antiliant) reste, à cette échelle élargie, le
premier descripteur de tout le projet dont la corrélation globale
survit à l'intégralité des stratifications testées} --- covalent,
ionique, métallique, mixte, \texttt{is\_metal} des deux côtés, tous
significatifs à $p<0{,}001$. \textbf{$\Delta$(ICOBI antiliant) a
lui aussi progressé} : il survit sur \emph{toutes} les
strates également (contre seulement métallique et \texttt{is\_metal}
avant), avec le résultat le plus fort sur \texttt{mixte}
($\rho=-0{,}840$). Ce n'est le cas d'\emph{aucun} autre descripteur de
ce rapport : la population antiliante brute (\S\ref{sec:antibonding})
a perdu son unique strate survivante (covalent, rétracté) et n'en garde
que deux (mixte, \texttt{is\_metal=False}) ; l'ICOHP de réaction
(\S\ref{sec:reaction-icohp}) ne survit que sur \texttt{bond\_type},
pas sur métallique. Reste une construction relativement récente --- sa
magnitude devra être reproduite sur un jeu de données véritablement
indépendant avant d'être prise comme un résultat définitivement acquis,
mais la survie à une stratification complète, maintenant à $n=434$ et
sur cinq sous-groupes au lieu de trois, en fait le résultat le plus
solide du projet.

\subsection{$\Delta$(ICOHP) de réaction restreint à une fenêtre proche de $E_F$}
\label{sec:reaction-icohp-windowed}

Extension proposée en conclusion (\S\ref{sec:reframing}) : plutôt que
l'ICOHP intégré sur toute la plage occupée (\S\ref{sec:reaction-icohp})
ou la seule partie antiliante près de $E_F$ (sous-sections précédentes),
une troisième construction --- l'ICOHP \emph{signé} (liant \emph{et}
antiliant, non tronqué) mais intégré \emph{uniquement} sur la fenêtre à
sens unique $(E_F-\Delta E,\ E_F]$, $\Delta E=1{,}0$~eV, sommé sur
\emph{toutes} les étiquettes de liaison (extensif, comme la somme ICOHP
complète) plutôt que sur la seule trace \og{}average\fg{} --- puis
combinée par la même stœchiométrie de réaction que le $\Delta$(ICOHP)
complet (\texttt{reaction\_icohp.py}, convention réactif $-$ produits
prédits). Implémentation :
\texttt{cohp\_extraction.icohp\_windowed\_per\_atom()} lit directement la
différence entre deux points de la courbe ICOHP \emph{cumulative} déjà
calculée par LOBSTER dans \texttt{COHPCAR.lobster}
(\texttt{ICOHP\_cumulative}$(E_F)$ $-$
\texttt{ICOHP\_cumulative}$(E_F-\Delta E)$, par étiquette), sans nouvelle
intégration trapézoïdale --- validé : à $\Delta E$ très grand (couvrant
toute la fenêtre de calcul), cette quantité retrouve la somme ICOHP
complète à $\sim$0,1\,\% près. Calculé pour 439 réactions de cas 1.

\begin{table}[htbp]
\centering
\caption{Corrélations de Spearman, $\Delta$(ICOHP fenêtré près de $E_F$) contre \texttt{formation\_energy\_per\_atom} (cas 1, $n=286$--$434$).}
\label{tab:reaction-icohp-windowed}
\begin{tabular}{@{}lrr@{}}
\toprule
Sous-groupe & $n$ & $\rho$ \\
\midrule
tous                & 434 & $+0{,}3566^{***}$ \\
\texttt{covalent}   & 29  & $+0{,}6185^{***}$ \\
\texttt{ionique}    & 87  & $+0{,}6184^{***}$ \\
\texttt{métallique} & 286 & $+0{,}1801^{**}$ \\
\texttt{mixte}      & 32  & $+0{,}4578^{**}$ \\
\texttt{is\_metal=False} & 148 & $+0{,}5643^{***}$ \\
\texttt{is\_metal=True}  & 286 & $+0{,}1801^{**}$ \\
\bottomrule
\end{tabular}

\vspace{0.15cm}
{\footnotesize $^{**}p<0{,}01$ ; $^{***}p<0{,}001$ ; aucune étoile = non
significatif. Contre \texttt{energy\_above\_hull} : $\rho=+0{,}073$,
$p=0{,}13$ au global, seule \texttt{mixte} atteint la significativité
($\rho=+0{,}497$, $p=0{,}004$, $n=32$) --- même comportement que les
autres descripteurs ICOHP/ICOBI de ce rapport contre cette cible.}
\end{table}

\textbf{À cette échelle élargie, cette construction survit elle aussi
sur l'intégralité des sous-groupes testés} ($p<0{,}01$ partout) ---
contrairement à l'ICOHP de réaction complet (\S\ref{sec:reaction-icohp}),
qui perd toute stratification \texttt{bond\_type}, et contrairement à
son propre comportement à échelle réduite (où seuls ionique et
\texttt{is\_metal} survivaient). Moins forte en magnitude que
$\Delta$(ICOHP antiliant) (sous-section précédente) sur la plupart des
strates, mais tout aussi générale dans sa survie à la
stratification. Corrélation croisée avec $\Delta$(ICOHP antiliant) :
$\rho=-0{,}567$ ($p<0{,}001$, $n=439$) --- fortement \emph{anti}corrélées
malgré leur construction proche (même fenêtre, même $E_F$), signe que
la troncature à la seule partie antiliante (sous-section précédente)
capture une information qualitativement différente de l'ICOHP signé
complet dans la même fenêtre. Corrélation croisée avec l'ICOHP de
réaction complet : $\rho=+0{,}241$ ($p<0{,}001$) --- liée mais nettement
distincte, pas une simple restriction de la même quantité.

\section{Structures des réactions de Reitz \& Dronskowski}
\label{sec:appendix-reitz-dronskowski-structures}

La validation de \S\ref{sec:reframing} (Tableau~\ref{tab:reference-validation})
ne compare que l'arithmétique des valeurs ICOHP publiées dans le
manuscrit --- indépendante de toute structure cristalline locale. Le
tableau ci-dessous documente, pour chacune des 16 espèces impliquées
dans ces 7 réactions, la structure telle que décrite par le manuscrit
(quand il la précise), l'entrée Materials Project/COD retenue dans ce
jeu de données, et si une vérification indépendante a pu être faite.
Trois cas ont révélé une erreur ou une ambiguïté réelle, corrigée
depuis : N$_2$ (phase polymérique théorique substituée par un dimère
gazeux isolé, \S\ref{sec:reframing}), Pb(N$_3$)$_2$ (mauvais polymorphe
Pnma substitué par le Pna2$_1$ cité par le manuscrit) et l'étain
compétiteur de ZnSn ($\beta$-étain substitué par l'$\alpha$-étain).

{\small
\input{appendix_reitz_dronskowski_structures_fr.tex}
}

\end{document}
